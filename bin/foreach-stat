#!/bin/sh

TEMPLATE=$1
if [ -z "$TEMPLATE" ]; then
  echo need template
  exit 1
fi

i=0
while read file title; do
  if [ $i != 0 ]; then
    echo -n ,
  else
    i=1
  fi
  echo $TEMPLATE | sed -e "s/__FILE__/$file/g" -e "s/__TITLE__/$title/g"
done <../titles
